module.exports = {
  verifyObject: async (jsonOutput) => {
    // verify if File is version openapi 3.0.0
    // check if Paths Exist
    // check if servers exist
    // check if Paths have HTTP Verbs (at least 1)
    // check if schema per content per path exists
  },
  identifyPaths: () => {

  },
  identifyComponents: () => {

  },
  identifySchemas: () => {

  }
};